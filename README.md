# Mobile Appdev Project Baseline

## Dev setup

1. Run `npm i`
2. Use two terminal, one runs `cd mockserver/ && npx json-server db.json5`, another run `npx ionic serve`.

Note: If using github codespace, need to change the baseURL in the `src/app/api/foodcatalog.ts` to the port forward mapped URL.

